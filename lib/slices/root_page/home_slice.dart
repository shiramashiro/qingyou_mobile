import 'package:flutter/material.dart';

class HomeSlice extends StatefulWidget {
  const HomeSlice({Key? key}) : super(key: key);

  @override
  State<HomeSlice> createState() => _HomeSliceState();
}

class _HomeSliceState extends State<HomeSlice> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
